<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="ProgressBar" parent="Widget.AppCompat.ProgressBar.Horizontal">
        <item name="android:scaleY">2</item>
    </style>

    <style name="StatSlider" parent="Base.Widget.MaterialComponents.Slider">
        <item name="thumbRadius">8dp</item>
        <item name="android:stepSize">10</item>
        <item name="android:valueFrom">0.0</item>
        <item name="android:valueTo">300.0</item>
    </style>

</resources>
